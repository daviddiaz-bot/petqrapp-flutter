import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Test básico - se puede expandir después
    expect(true, true);
  });
}
